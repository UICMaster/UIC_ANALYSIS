async function createOrUpdateEvent(matchData) {
    // Placeholder: Authenticates as Bot and creates Guild Scheduled Event
    console.log(`   -> [Discord] Creating event for ${matchData.myTeam}`);
}

module.exports = { createOrUpdateEvent };
