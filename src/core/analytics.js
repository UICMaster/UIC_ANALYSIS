function calculateGD15(playerTimeline, enemyTimeline) {
    // Placeholder: Math for Gold Diff at 15
    return 0;
}

function calculateDamageEfficiency(damage, gold) {
    // Placeholder: Math for Damage/Gold
    return (damage / gold).toFixed(2);
}

module.exports = { calculateGD15, calculateDamageEfficiency };
